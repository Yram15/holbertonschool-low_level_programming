#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: length
 */
int __strlen(char *s);
{
int login = 0;
while (*s != '\0')
{
login++;
S++;
}
return (longi);
}
